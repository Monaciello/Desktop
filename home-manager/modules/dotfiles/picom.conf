backend = "xrender";

# opacity
inactive-opacity = 1.0;
active-opacity = 1.0;
frame-opacity = 1.0;
opacity-rule = [
    "85:class_g = 'kitty'",
    "90:class_g = 'obsidian'"
];

shadow = false;
fading = false;
vsync = true;

# Fixes for VM environments
unredir-if-possible = false;
