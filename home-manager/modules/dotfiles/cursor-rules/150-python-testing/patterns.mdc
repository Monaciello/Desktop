**Naming:** `test_<unit>_<scenario>_<expected_behavior>`

**One assert per test:** Each test has single purpose. Multiple assertions = unclear failure.

**Parametrize:** Use `@pytest.mark.parametrize` for data-driven tests.

**Lookup:** Use Serena to find existing test patterns in the codebase. Refer to pytest docs for fixtures and parametrize.
