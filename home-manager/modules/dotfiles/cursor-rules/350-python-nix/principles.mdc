**Separation:** Nix manages interpreter version, system C libraries, shell environment. uv manages Python packages, virtualenv, lockfile.

**Why flakes:** `import <nixpkgs>` is impure. Flakes pin inputs via `flake.lock`. Multi-system support is first-class.

**Why uv:** Fast, native lockfile support, standard `pyproject.toml` compatibility.
