---
description: "Python project Nix environments: devShells, packaging, uv integration, cross-platform"
globs: ["**/flake.nix", "**/shell.nix", "**/shells/**/*.nix"]
alwaysApply: false
---

# Python + Nix Integration

## Core Rules

1. **Flakes only** — No `import <nixpkgs>`. All Python projects use flakes with `flake-parts`.
2. **uv is exclusive** — Never `pip`, `poetry`, or `pip-tools`. `pyproject.toml` is the single source of truth.
3. **Nix provides the shell** — devShells supply interpreter + system libs only. Python deps come from `uv`.
4. **Cross-platform** — Support Linux and Darwin with `lib.optionals`.

## Minimal Pattern

```nix
{
  description = "Python project";
  inputs = {
    nixpkgs.url = "github:nixos/nixpkgs/nixos-unstable";
    flake-parts.url = "github:hercules-ci/flake-parts";
  };
  outputs = { flake-parts, ... }@inputs:
    flake-parts.lib.mkFlake { inherit inputs; } {
      systems = [ "x86_64-linux" "aarch64-linux" "aarch64-darwin" "x86_64-darwin" ];
      perSystem = { pkgs, lib, ... }: {
        devShells.default = pkgs.mkShell {
          packages = [
            pkgs.python313
            pkgs.uv
          ] ++ lib.optionals pkgs.stdenv.isLinux [
            pkgs.stdenv.cc.cc.lib  # libstdc++
            pkgs.zlib
          ] ++ lib.optionals pkgs.stdenv.isDarwin [
            pkgs.darwin.apple_sdk.frameworks.Security
          ];
          shellHook = ''
            uv sync
            source .venv/bin/activate
          '';
        };
      };
    };
}
```

## What Goes Where

| Managed by | File |
|------------|------|
| Python package versions | `pyproject.toml` |
| System libs (C deps) | `flake.nix` |
| Python interpreter version | `flake.nix` |
| Virtual environment | `.venv/` (gitignored) |

## Common System Deps

| Python Package | Nix Libs |
|----------------|----------|
| `numpy`, `scipy` | `pkgs.blas`, `pkgs.lapack` |
| `cryptography` | `pkgs.openssl`, `pkgs.libffi` |
| `lxml` | `pkgs.libxml2`, `pkgs.libxslt` |
| `pillow` | `pkgs.libjpeg`, `pkgs.zlib` |
| `psycopg2` | `pkgs.postgresql` |

## Verification

```bash
nix flake check           # Validate
nix develop               # Enter shell
python --version && uv --version
```
